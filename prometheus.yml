global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'network_traffic_metrics'
    static_configs:
      # "docker-host" is a name defined in compose.yml
      - targets: ['docker-host:8000']

