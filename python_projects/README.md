# Python projects

This directory contains Python scripts that can be run in the same Docker container. This is used to get some weight off from the infrastructure, so we wouldn't need a separate container for each script.
